$('#memo').html("<%= j(render 'memos/new', memos: @memos) %>");
$("text_area").val("");
$("#text").val("");

$('#comment').html("<%= j(render 'comments/new', comments: @comments) %>");
$("text_area").val("");
$("#text").val("");

